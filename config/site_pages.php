<?php

/*
| Marketing page copy lives in resources/content/site_pages/{section}.php
| and is lazy-loaded via App\Support\SitePages (one section per request).
|
| Do not put bulk page content here — it would load on every request.
*/

return [];
